/** App shell — phone-first, expands on tablet/desktop. */
export const APP_SHELL_WIDTH_CLASS =
  "w-full max-w-[430px] md:max-w-4xl lg:max-w-6xl xl:max-w-7xl";

/** Horizontal page padding that scales with viewport. */
export const APP_PAGE_PADDING_X = "px-4 md:px-6 lg:px-8";

/** Modal / bottom sheet width — aligned with shell on larger screens. */
export const APP_MODAL_WIDTH_CLASS =
  "w-full max-w-[430px] md:max-w-lg lg:max-w-xl";

/** Discover / profile grid columns. */
export const PROFILE_GRID_CLASS =
  "grid grid-cols-2 gap-2.5 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6 md:gap-3 lg:gap-4";
